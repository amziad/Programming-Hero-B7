var friendName = ['Ziad', 'Fuad', 'Asha', 'Nahid'];
var friendAge = [25, 32, 28, 27];
var friendIncome = [500, 2000, 0, 1000];

console.log(friendName, friendIncome, friendAge);

// Array Length
console.log(friendName.length, friendIncome.length, friendAge.length);