// servedMeal = servedMeal + 1;
// servedMeal += 1;
// servedMeal++;

/*
1. Loop variable
2. Condition inside loop
3. Loop body
4. Change the loop variable
*/ 

var servedMeal = 0;

while (servedMeal < 5){
    console.log('Serve meal please.');
    console.log(servedMeal);
    servedMeal++;
};